#include <bits/stdc++.h>

using namespace std;

int main(){
	string s;
	cin >> s;
	string s2;
	int cnt = 0, str = 1;
	for(int i = 0; i < s.size(); i++){
      s2.substr(i, str );
      if(s2 == )
	}
}